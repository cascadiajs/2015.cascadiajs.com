<div class="sponsor col-sm-4 <%= index % 2 == 0 ? 'pull-left' : 'pull-right' %>">
  <a href="<%= url %>" class="logo">
    <img src="<%= logo %>" alt="<%= name %> logo"/>
  </a>
  <h4 class="name"><a href="<%= url %>"><%= name %></a></h4>
  <p class="description"><%= description %></p>
</div>