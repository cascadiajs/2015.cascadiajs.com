
<div id="browserday" class="cjsf-day">
  <div class="banner">
    <div class="container-fluid">
      <div class="row">
        <div class="col-xs-12">
          <h1>Cascadia<span class="thin">JS</span> Browser Day</h1>
          <p class="left"><%- description %></p>
        </div>
      </div>
    </div>
  </div>

  <div class="content">
    <%
    if(typeof schedule !== 'undefined') { %>
      <%- partial("../_partials/day/schedule", schedule) %>
    <% }
    %>

    <%
    if(typeof speakers !== 'undefined') { %>
      <%- partial("../_partials/day/speakers", speakers) %>
    <% }
    %>

    <div class="content">
      <div class="container-fluid">
        <div class="row">
          <div class="col-xs-12">
    <p>If it happens in the the browser, we want to know about it. Components, the Shadow DOM, web workers, visualizations and WebRTC are all coming to the party. And a few special surprise guests...</p>
          </div>
        </div>
      </div>
    </div>

    <%
    if(typeof sponsors !== 'undefined') { %>
      <%- partial("../_partials/day/sponsors", sponsors) %>
    <% }
    %>

    <%
    if(typeof staff !== 'undefined') { %>
      <%- partial("../_partials/day/staff", staff) %>
    <% }
    %>

  </div>
</div>